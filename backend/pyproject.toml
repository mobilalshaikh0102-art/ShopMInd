[project]
name = "shopmind-backend"
version = "1.0.0"
description = "ShopMind AI-Powered E-Commerce Operations Platform Backend"
requires-python = ">=3.11"

[tool.uv]
dev-dependencies = ["pytest>=8.0", "httpx>=0.27", "pytest-asyncio>=0.23"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
